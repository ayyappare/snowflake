seeds:
  - name: student_data
    config:
      column_types:
        std_id: number
        std_name: varchar(30)
        std_grade: float
        std_heaight: float
        std_weight: float
        country:
          division:
            column_types:
              SUBDIVISION: varchar(100)
              year: date
              jan: float
              feb: float
              mar: float

